@{
    ViewData["Title"] = "MyCart";
    decimal? toplamTutar = 0;
}

<div class="row mt-3">
    <div class="col-md-9">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Product Name</th>
                    <th>UnitPrice</th>
                    <th>Quantity</th>
                    <th>SubTotal</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in ViewBag.Cart._myCart.Values)
                {
                    toplamTutar += item.SubTotal;
                    <tr>
                        <td>@item.ProductName</td>
                        <td>@item.UnitPrice</td>
                        <td>@item.Quantity</td>
                        <td>@item.SubTotal</td>
                    </tr>
                }
            </tbody>
        </table>

    </div>
    <div class="col-md-3">
        <div class="card">
            <h1>Toplam Tutar: @toplamTutar ₺</h1>
        </div>
    </div>
</div>

